info:
  name: create todo
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/todos
  body:
    type: json
    data: |-
      {
        "title": "todo6"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
