info:
  name: sign in
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/auth/sign-in
  body:
    type: json
    data: |-
      {
        "email": "admin@api.com",
        "password": "adminadmin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
